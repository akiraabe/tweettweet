type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  profilePic: AWSURL
  name: String
  handle: String
}

type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  content: String
  postedAt: AWSDateTime
  likes: Int
  User: User @hasOne
}
 